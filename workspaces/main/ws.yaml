name: main-workspace
# Make sure that the project and domain exists
project: default
domain: development
container_image: docker.io/unionbio/main:tI6cc_wwnjeLjhzNyt_Z_g

resources:
    cpu: "2"
    mem: "4Gi"
    gpu: "1"

metadata: null
on_startup: null
